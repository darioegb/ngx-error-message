import { AvoidMutipleZeroValidatorDirective } from './avoid-mutiple-zero-validator.directive';

describe('AvoidMutipleZeroValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AvoidMutipleZeroValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
